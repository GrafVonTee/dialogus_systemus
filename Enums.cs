namespace DialogusSystemus
{
    public enum Alignment
    {
        Left,
        Center,
        Right
    }

    public enum Options
    {
        Default,
        Error,
        Focused,
        Chaptered,
        Sectioned,
        Heading
    }
    public enum Tag
    {
        Default,
        Name,
        Place,
        Reward
    }
}
