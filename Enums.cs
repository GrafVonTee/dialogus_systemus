namespace DialogusSystemus
{
    public enum Alignment
    {
        Left,
        Center,
        Right
    }

    public enum Options
    {
        Default,
        Error,
        Focused,
        Chaptered,
        Sectioned,
    }
    public enum Tag
    {
        Default,
        Name,
        Place,
        Reward
    }
}
