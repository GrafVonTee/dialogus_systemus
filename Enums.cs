namespace DialogusSystemus
{
    public enum Alignment
    {
        Left,
        Center,
        Right
    }

    public enum Answers
    {
        Greetings,
        NiceQuest,
        AngryQuest,
        LonglessQuest,
        TermQuest,
        Byes,
        Yes,
        No,
        Proposal
    }

    public enum Options
    {
        Default,
        Error,
        Focused,
        Chaptered,
        Sectioned,
    }
    public enum Tag
    {
        Default,
        Name,
        Place,
        Reward
    }
}
